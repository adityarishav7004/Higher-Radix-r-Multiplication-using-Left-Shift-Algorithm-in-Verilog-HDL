# Higher-Radix-r-Multiplication-using-Left-Shift-Algorithm-in-Verilog-HDL
A generalized higher radix multiplication of radix r that uses left shift algorithm to perform the multiplication operation by decreasing the number of partial products.
